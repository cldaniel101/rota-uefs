[pytest]
minversion = 7.0
asyncio_mode = auto
testpaths = .
python_files = test_*.py *_test.py
addopts = --strict-markers --cov=app --cov-report=term-missing -p pytest_asyncio
markers =
    integration: marks tests as integration tests
    asyncio: marks tests as asyncio coroutines
